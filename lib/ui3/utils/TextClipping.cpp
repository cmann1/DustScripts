enum TextClipping
{
	
	/** Text is visible outside of the text element bounding box. */
	None,
	/** Text is not drawn outside of the text element bounding box. */
	Hidden,
	/** Truncate the text and display ellipses. */
	Ellipses,
	
}
