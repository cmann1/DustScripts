namespace ClippingMode
{
	
	// No clipping occurs
	const int None		= 0;
	// An element will be clipped if it is not completely contained within the clipping region
	const int Inside	= 1;
	// An element will be clipped only when it is completely outside of the clipping region
	const int Outside	= 2;
	
}